{
  "_comment": [
    "THE one declaration of every setting the remote environment may set.",
    "'env' is the variable the R drivers actually read; 'name' is the config field.",
    "'scope' is the important column:",
    "  model       changes WHAT IS ESTIMATED. Two runs differing in a model knob are not comparable,",
    "              and the defaults here are the VALIDATED standard -- the evidence for each lives in",
    "              docs/sampler_model_specification.md. Deviating is legitimate; doing so unknowingly",
    "              is not, so the run log and manifest flag every model knob set away from standard.",
    "  operational changes where things go, how long they run, how many cores. Never affects results.",
    "'tasks' is which task FAMILY a knob reaches: nested | flat | project | count. It is not cosmetic -- a nested-only knob offered on a project task is a control that does nothing, and a control that does nothing is indistinguishable from one that is broken."
  ],
  "knobs": [
    {
      "name": "NITER",
      "env": "DRIVER_NITER",
      "type": "integer",
      "default": "1000",
      "scope": "operational",
      "desc": "Total MCMC sweeps.",
      "tasks": [
        "nested",
        "flat",
        "project",
        "count"
      ]
    },
    {
      "name": "NBURN",
      "env": "DRIVER_NBURN",
      "type": "integer",
      "default": "",
      "scope": "operational",
      "desc": "Burn-in sweeps; must be < NITER. Empty keeps the driver default.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "THIN",
      "env": "DRIVER_THIN",
      "type": "integer",
      "default": "",
      "scope": "operational",
      "desc": "Store every k-th retained draw.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "N_CHAINS",
      "env": "DRIVER_NCHAINS",
      "type": "integer",
      "default": "1",
      "scope": "operational",
      "desc": "Chains fitted in parallel; Rhat needs at least 2.",
      "tasks": [
        "nested",
        "flat",
        "count"
      ]
    },
    {
      "name": "SUBSAMPLE",
      "env": "NCUT_SUB",
      "type": "integer",
      "default": "0",
      "scope": "operational",
      "desc": "0 = every pixel; otherwise cap the pixel count.",
      "tasks": [
        "nested"
      ]
    },
    {
      "name": "CLASSIFICATION",
      "env": "DRIVER_CLASS_COLS",
      "type": "string",
      "default": "GLOBIOM_subclass",
      "scope": "model",
      "enum": [
        "GLOBIOM_subclass",
        "GLOBIOM_UNFCCC,GLOBIOM_mngmt",
        "AgMIP_label",
        "BIOCLIMA_DS_reporting",
        "BMLEH_Los1_label"
      ],
      "desc": "Class column(s) the model predicts -- decides what the outcome IS.",
      "tasks": [
        "flat",
        "nested"
      ]
    },
    {
      "name": "RE_GROUP",
      "env": "DRIVER_RE_GROUP_COL",
      "type": "string",
      "default": "GLOB_country",
      "scope": "model",
      "enum": [
        "GLOB_country",
        "CAPRI_NUTS",
        "NUTS2",
        "NUTS3"
      ],
      "desc": "Grouping the random effects are keyed on.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "RE_VARS",
      "env": "DRIVER_RE_VARS",
      "type": "string",
      "default": "",
      "scope": "model",
      "desc": "Covariates carrying a random effect. Empty = intercept only (the standard; measured to converge better than random slopes). 'all' = every linear covariate; or a comma-separated list.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "RE_BLOCK",
      "env": "NCUT_RE_COLS",
      "type": "string",
      "default": "intercept",
      "scope": "model",
      "enum": [
        "intercept",
        "intercept+socio"
      ],
      "desc": "Nested-path RE block. intercept is the documented standard.",
      "tasks": [
        "nested"
      ]
    },
    {
      "name": "VARIANT",
      "env": "NCUT_USE_IV",
      "type": "string",
      "default": "factorized",
      "scope": "model",
      "enum": [
        "factorized",
        "iv"
      ],
      "desc": "factorized = independent nests; iv = inclusive-value coupling.",
      "tasks": [
        "nested"
      ]
    },
    {
      "name": "SYMMETRIC_HS",
      "env": "NCUT_SYM_HS",
      "type": "string",
      "default": "TRUE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Symmetric (zero-sum) horseshoe. TRUE is the standard; the job verifies the sampler carries the fix before fitting.",
      "tasks": [
        "nested"
      ]
    },
    {
      "name": "USE_BART",
      "env": "DRIVER_USE_BART",
      "type": "string",
      "default": "FALSE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Terrain (and whatever BART_COLS names) enters as a tree ensemble instead of linear terms. FLAT PATH ONLY -- nested_cut has no BART support.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "BART_COLS",
      "env": "DRIVER_BART_COLS",
      "type": "string",
      "default": "topo",
      "scope": "model",
      "desc": "'topo' = the terrain block plus lon/lat when ADD_COORDS; '' = every non-focal column; or an explicit comma-separated list.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "NTREES_BART",
      "env": "DRIVER_NTREES_BART",
      "type": "integer",
      "default": "25",
      "scope": "model",
      "desc": "Trees per class in the ensemble.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "BART_K_PREV",
      "env": "DRIVER_BART_K_PREV",
      "type": "string",
      "default": "TRUE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Prevalence-scaled leaf shrinkage. Measured super-additive with symmetric BART (+75.4 held-out together vs +38.4 symmetric alone); FALSE restores flat k.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "ADD_COORDS",
      "env": "DRIVER_ADD_COORDS",
      "type": "string",
      "default": "FALSE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Add lon/lat as design columns. REQUIRED for BART_COLS=topo to cover coordinates -- without it 'topo' silently resolves to the 4 terrain columns.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "COORD_TYPE",
      "env": "DRIVER_COORD_TYPE",
      "type": "string",
      "default": "lonlat",
      "scope": "model",
      "enum": [
        "lonlat",
        "laea"
      ],
      "desc": "lonlat = EPSG:4326 degrees (readable); laea = native EPSG:3035 metres.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "MUNDLAK",
      "env": "DRIVER_MUNDLAK",
      "type": "string",
      "default": "FALSE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Group-mean predictors for the RE prior mean. An interpretation fix (removes FE-RE correlation), not an accuracy one.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "PROMOTE_NATURAL_OTHER",
      "env": "DRIVER_PROMOTE_NATURAL_OTHER",
      "type": "string",
      "default": "TRUE",
      "scope": "model",
      "enum": [
        "TRUE",
        "FALSE"
      ],
      "desc": "Move Natural_other out of the non-choosable residual.",
      "tasks": [
        "flat"
      ]
    },
    {
      "name": "N_CORES",
      "env": "DRIVER_NCORES",
      "type": "integer",
      "default": "",
      "scope": "operational",
      "desc": "Worker processes. Empty follows N_CHAINS; more cores than chains does not help.",
      "tasks": [
        "nested",
        "flat",
        "count"
      ]
    },
    {
      "name": "M",
      "env": "NCUT_M",
      "type": "integer",
      "default": "25",
      "scope": "model",
      "desc": "Multiple-imputation draws of the inclusive value. Only reaches the model when VARIANT=iv; with factorized nests there is no inclusive value to impute.",
      "tasks": [
        "nested"
      ]
    },
    {
      "name": "BM_NITER",
      "env": "BM_NITER",
      "type": "integer",
      "default": "",
      "scope": "operational",
      "desc": "Sweeps for a project fit. Empty falls back to NITER, then to the project script's own 5000.",
      "tasks": [
        "project"
      ]
    }
  ]
}
